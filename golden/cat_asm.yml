in_source: |-
  _start:
      ei
      push 0
      load_symbol
      push 10
      sub
      jz print
      jmp _start
  print:
      load
      store
      halt
in_stdin: |-
  [(1,'c'),(1,'a'),(3,'t'),(4,'\n')]
out_code: |-

out_stdout: |-

out_log: |-