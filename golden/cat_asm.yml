in_source: |-
  _start:
      ei
      load_symbol
      push 10
      sub
      jz print
      jmp _start
  print:
      load
      store
      halt
in_stdin: |-

out_code: |-

out_stdout: |-

out_log: |-