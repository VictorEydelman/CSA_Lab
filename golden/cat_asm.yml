in_source: |-
  _start:
      push 0
  loop:
      ei
      load_symbol
      push 10
      sub
      jz print
      jmp loop
  print:
      load
      store
      halt
in_stdin: |-

out_code: |-

out_stdout: |-

out_log: |-